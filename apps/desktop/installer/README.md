# Installer (planned)

Reserved for Windows/macOS packaging (e.g. WiX, MSIX, DMG) that bundles:

- Python runtime or embedded backend
- Node/Electron shell
- Default `models/` directory and config

No installer definitions are generated in this milestone.
